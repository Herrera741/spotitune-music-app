# spotitune-music-app

Full-stack Spotify clone app for iOS with audio interruption handler, lock screen controls. MySQL database with PHP scripts automated via Linux commands to auto-insert non-existing files.
